// swift-tools-version:5.3
import PackageDescription

let package = Package(
  name: "Switch",
  products: [
    .library(
      name: "Switch",
      targets: ["Switch"]),
  ],
  dependencies: [
  ],
  targets: [
    .target(
      name: "Switch",
      dependencies: []),
  ]
)
